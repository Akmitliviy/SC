import { PipeEntityPipe } from './pipe-entity.pipe';

describe('PipeEntityPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeEntityPipe();
    expect(pipe).toBeTruthy();
  });
});
