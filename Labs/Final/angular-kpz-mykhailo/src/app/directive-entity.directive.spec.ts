import { DirectiveEntityDirective } from './directive-entity.directive';

describe('DirectiveEntityDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveEntityDirective();
    expect(directive).toBeTruthy();
  });
});
